# D5 MVE deps.
# Offline plumbing check (python run.py / run_real.py with mock backend): stdlib only, NOTHING required.

# OpenRouter (black-box agents+judge) path:
requests>=2.31

# Local HF open-weight Worker path (1x 24GB GPU, e.g. RTX 4090):
# torch>=2.3
# transformers>=4.44
# accelerate>=0.33
# sentence-transformers>=3.0        # optional: embedding goal-hijack oracle (BAAI/bge-small-en-v1.5)

# Real MAS apps (the testbed) — see ARTIFACTS.md; AutoGen is the substrate FLARE used:
# autogen-agentchat>=0.4
# autogen-ext[openai]>=0.4

# Real stats for the head-to-head (replace the dependency-free Wilcoxon stub):
# scipy>=1.11
